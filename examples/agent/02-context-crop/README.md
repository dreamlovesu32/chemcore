# 02 Context Crop

This example queries the area around a reaction arrow object and renders the
same context bounds as a PNG.

```powershell
.\one-shot.ps1
```

Outputs:

- `context.json`: nearby object ids, spatial relation, group/link metadata, and
  selection-box membership.
- `context.png`: high-resolution crop of the same context bounds.
